def find_value(nums1, nums2, m, n, index):
    nums1.append(0)
    nums2.append(0)
    while True:
        pass


def find_median_sorted_arrays(nums1, nums2):
    m = len(nums1)
    n = len(nums2)
    if m + n % 2 == 0:
        mid = m + n / 2
    else:
        pass

